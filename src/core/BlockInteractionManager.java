package core;

import entities.Player;
import map.GameMap;

public class BlockInteractionManager {
    private GameMap map;
    private Player player;

    public BlockInteractionManager(Player player, GameMap map) {
        this.player = player;
        this.map = map;
    }
    public void handleInteraction() {
    //TODO
    }
}
